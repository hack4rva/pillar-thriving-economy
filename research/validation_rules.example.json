{
  "_comment": "Copy to validation_rules.json and adjust regexes for conflict checks.",
  "fact_patterns": [
    {
      "key": "mbe_threshold",
      "regex": "(?i)(MBE|minority[- ]owned).*?threshold[:\- ]+\$?(?P<val>[\d,]+)"
    }
  ]
}

